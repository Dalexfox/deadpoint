// App-wide constants.

// Set to the @deadpoint profile UUID once the official account exists —
// new signups will then auto-follow it. While null, signup creates no follow.
export const OFFICIAL_ACCOUNT_ID: string | null = null;
